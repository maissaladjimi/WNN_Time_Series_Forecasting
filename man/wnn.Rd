% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wnn.R
\name{wnn}
\alias{wnn}
\title{Weighted Nearest Neighbors Forecasting}
\usage{
wnn(
  y,
  X = NULL,
  m,
  k = 10,
  h = 1,
  X_future = NULL,
  scale = TRUE,
  distance = "euclidean",
  weight_func = "inverse",
  return_all = FALSE,
  plot = TRUE,
  temp_lags = 1
)
}
\arguments{
\item{y}{Numeric vector. Target time series (e.g., electricity consumption)}

\item{X}{Numeric vector or matrix. Exogenous variables (e.g., temperature).
Optional. Default = NULL}

\item{m}{Integer. Embedding dimension (window size).
Recommended: 96 for daily patterns with 15-min data}

\item{k}{Integer. Number of nearest neighbors. Default = 10}

\item{h}{Integer. Forecast horizon (number of steps ahead). Default = 1}

\item{X_future}{Numeric vector or matrix. Future exogenous variables.
Required if h > 1 and X is provided. Must have at least h rows.}

\item{scale}{Logical. Should embeddings be standardized? Default = TRUE}

\item{distance}{Character. Distance metric: "euclidean" or "manhattan".
Default = "euclidean"}

\item{weight_func}{Character. Weighting function: "inverse" or "inverse_squared".
Default = "inverse"}

\item{return_all}{Logical. Return detailed diagnostics? Default = FALSE}

\item{plot}{Logical. Should a plot of historical and forecasted values be shown? Default = TRUE}

\item{temp_lags}{Integer. Number of temperature lags to use. Default = 1 (current only)}
}
\value{
If h = 1: List with forecast value and diagnostics
If h > 1 and return_all = FALSE: Numeric vector of forecasts
If h > 1 and return_all = TRUE: List with forecasts and detailed diagnostics
}
\description{
Main wrapper function for WNN forecasting. Automatically handles both
single-step and multi-step forecasting with optional scaling.
}
